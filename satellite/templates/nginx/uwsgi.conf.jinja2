server {
    listen {{ port }};
    server_name {{ server_name }};

    root {{ public_dir }};
    location ~ /({{ static_dirs|join('|') }})/ {
        root {{ public_dir }};
    }

    location / {
        uwsgi_pass {{ uwsgi_pass }};
        include uwsgi_params;
    }
}
